const AdminDashboard = () => {
  return (
    <main className="min-h-screen container mx-auto py-10">
      <h1 className="text-3xl font-semibold mb-2">Admin Panel</h1>
      <p className="text-muted-foreground">Manage users, services, payments, and disputes. (Connect backend to enable.)</p>
    </main>
  );
};

export default AdminDashboard;
