const ProviderDashboard = () => {
  return (
    <main className="min-h-screen container mx-auto py-10">
      <h1 className="text-3xl font-semibold mb-2">Provider Dashboard</h1>
      <p className="text-muted-foreground">Accept/reject jobs, update status, and track earnings. (Connect backend to enable.)</p>
    </main>
  );
};

export default ProviderDashboard;
